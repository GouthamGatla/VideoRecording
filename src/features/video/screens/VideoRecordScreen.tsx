import { View } from "react-native"
import { VideoRecorderScreenComponent } from "../../../components/VideoRecorderComponent"

export const VideoRecordScreen = () => {
    return (

        <VideoRecorderScreenComponent />

    )
}